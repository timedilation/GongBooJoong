#include <iostream>
using namespace::std;

int main()
{
    int n, arr[2][3];
    cin >> n;

    for(int i=0; i<n; i++) {
        int num;
        cin >> num;
        arr[0][i%3] = arr[0][(i+2)%3] +
    }

}
